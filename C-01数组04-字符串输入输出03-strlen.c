#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>
#include <math.h>
#define SIZE 100

int main(int argc, char const *argv[])
{ //计算字符串有效长度
    char ch[] = "hello world";
    int len = 0;
    while (ch[len])
    {
        ;
    }
    printf("字符串长度为%d\n", len);
    while (1)
    {
        // gets(ch);
        // printf("数组大小:%d\n", sizeof(ch));
        // printf("字符串长度:%d\n", strlen(ch));
        // printf("%s",ch);
        return 0;
    }
}